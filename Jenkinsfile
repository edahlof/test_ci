pipeline {
    agent { label 'built-in' }
    stages {
        stage('build') {
            steps {
                bat 'python --version'
                echo "Tjena mittbena!!!"
            }
        }
    }
}